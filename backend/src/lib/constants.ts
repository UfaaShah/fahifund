// Default password used for the bootstrap Super Admin account (seed.ts) and
// for every new account created via Add Member (routes/users.ts). Everyone
// is expected to change it after first login via Profile → Change Password.
export const DEFAULT_PASSWORD = "welcome123";
